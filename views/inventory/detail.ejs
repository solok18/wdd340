<!-- Content Heading -->
 <% if (title) { %>
<h1><%= title %></h1>
<% } else {
    res.redirect('/')
} %>

<%# vehicle dteails %>
<%- detail %>

<% if (locals.loggedin) { %>
    <form action="/favorites/add" method="post" class="favorite-form">
        <input type="hidden" name="inv_id" id="inv_id" value="<%= data.inv_id %>">
        <button type="submit">Save to Favorites</button>
    </form>
<% } else { %>
    <p class="login-to-save"><a href="/account/login">Log in to add to favorites.</a></p>
<% } %>